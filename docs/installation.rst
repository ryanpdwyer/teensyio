============
Installation
============

At the command line via pip::

    $ pip install teensyio

Or install the latest GitHub version using::

    $ git clone https://github.com/ryanpdwyer/teensyio.git
    $ cd teensyio
    $ python setup.py install
